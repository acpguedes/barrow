{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#purpose","title":"Purpose","text":"<p><code>barrow</code> is evolving from a collection of Arrow-powered CLI subcommands into a compact analytical engine for tabular pipelines. The final architecture should preserve the current Unix-style ergonomics while adding a single execution model that can support CLI commands, SQL, future Python APIs, and optimized execution across multiple backends.</p> <p>This document defines the target architecture for that final optimized version.</p>"},{"location":"architecture/#product-goals","title":"Product goals","text":""},{"location":"architecture/#primary-goals","title":"Primary goals","text":"<ul> <li>Keep the CLI fast, scriptable, and composable with <code>STDIN</code>/<code>STDOUT</code>.</li> <li>Support a unified transformation model across CSV, Parquet, Feather, and ORC.</li> <li>Expose the same engine through multiple frontends: CLI first, SQL next, Python API later.</li> <li>Optimize common pipelines through planning instead of executing each command eagerly.</li> <li>Use Apache Arrow as the in-memory interoperability layer.</li> <li>Use DuckDB selectively for SQL, complex joins, sorting, and advanced window execution.</li> </ul>"},{"location":"architecture/#non-goals","title":"Non-goals","text":"<ul> <li>Building a distributed query engine.</li> <li>Replacing general-purpose data warehouses.</li> <li>Supporting arbitrary Python execution inside user expressions.</li> </ul>"},{"location":"architecture/#architectural-principles","title":"Architectural principles","text":"<ol> <li>One engine, many frontends. CLI, SQL, and future APIs must compile into the same logical plan.</li> <li>Lazy internally, explicit externally. Users keep the current CLI experience, but execution is deferred until the engine has a full plan to optimize.</li> <li>Arrow-native contracts. Arrow remains the canonical in-memory table format between layers.</li> <li>Backend polymorphism. The planner chooses the best execution backend for each plan or plan fragment.</li> <li>Metadata is logical state, not transport state. Grouping, ordering, and partition semantics should live in the logical plan rather than only in schema metadata.</li> <li>Format adapters are isolated. File-format detection, scan behavior, schema inference, and sink writing must remain outside core planning logic.</li> </ol>"},{"location":"architecture/#current-state-summary","title":"Current-state summary","text":"<p>The current codebase already has the right seeds for the target design:</p> <ul> <li>A thin CLI layer with subcommands for <code>filter</code>, <code>select</code>, <code>mutate</code>, <code>groupby</code>, <code>summary</code>, <code>ungroup</code>, <code>join</code>, and <code>view</code>.</li> <li>A separate <code>operations</code> package that contains transformation primitives.</li> <li><code>io.reader</code> and <code>io.writer</code> helpers that detect and serialize Arrow-compatible formats.</li> <li>An expression parser/evaluator used by row filtering and column mutation.</li> <li>Early support for <code>window</code> and <code>sql</code> operations in the operations layer, even though they are not yet fully surfaced as first-class CLI frontends.</li> </ul> <p>The final architecture formalizes and generalizes this structure.</p>"},{"location":"architecture/#target-architecture-overview","title":"Target architecture overview","text":"<pre><code>+-----------------------------+\n| Frontends                   |\n| CLI | SQL | Python API      |\n+-------------+---------------+\n              |\n              v\n+-----------------------------+\n| Frontend Adapters           |\n| cli_to_plan | sql_to_plan   |\n| api_to_plan                 |\n+-------------+---------------+\n              |\n              v\n+-----------------------------+\n| Core Logical Layer          |\n| LogicalPlan | Expr AST      |\n| Schema | LogicalProperties  |\n+-------------+---------------+\n              |\n              v\n+-----------------------------+\n| Optimizer                   |\n| pushdown | fusion |         |\n| simplification | backend    |\n+-------------+---------------+\n              |\n              v\n+-----------------------------+\n| Execution Engine            |\n| planner | scheduler |       |\n| materialization policy      |\n+------+------+---------------+\n       |      |\n       v      v\n+----------+  +---------------+\n| Arrow    |  | DuckDB        |\n| backend  |  | backend       |\n+----------+  +---------------+\n       \\         /\n        \\       /\n         v     v\n      +-----------------------+\n      | I/O Adapters          |\n      | CSV | Parquet |       |\n      | Feather | ORC         |\n      +-----------------------+\n</code></pre>"},{"location":"architecture/#layer-by-layer-design","title":"Layer-by-layer design","text":""},{"location":"architecture/#1-frontends","title":"1. Frontends","text":"<p>Frontends are responsible only for turning user input into a common internal representation.</p>"},{"location":"architecture/#cli-frontend","title":"CLI frontend","text":"<p>The CLI remains the flagship interface, but it stops executing operations directly. Instead, each subcommand builds a logical operator and appends it to a plan.</p> <p>Planned CLI capabilities:</p> <ul> <li>Existing commands: <code>filter</code>, <code>select</code>, <code>mutate</code>, <code>groupby</code>, <code>summary</code>, <code>ungroup</code>, <code>join</code>, <code>view</code></li> <li>New first-class commands: <code>sql</code>, <code>window</code>, <code>sort</code>, <code>limit</code>, <code>explain</code></li> <li>Shared I/O options with consistent semantics</li> <li>Pipeline awareness so chained commands can become one optimized logical graph</li> </ul>"},{"location":"architecture/#sql-frontend","title":"SQL frontend","text":"<p>SQL should become a first-class input mode, not just a direct DuckDB call. The preferred design is:</p> <ul> <li>Parse SQL into a logical plan when possible.</li> <li>Fall back to a DuckDB-backed physical plan for unsupported cases.</li> <li>Preserve output contracts with the rest of the engine.</li> </ul>"},{"location":"architecture/#python-api-frontend","title":"Python API frontend","text":"<p>A future Python API should expose fluent lazy operations such as:</p> <pre><code>(\n    barrow.scan(\"data.parquet\")\n    .filter(\"a &gt; 1\")\n    .mutate(\"c=a+b\")\n    .groupby([\"grp\"])\n    .summary({\"c\": \"sum\"})\n    .collect()\n)\n</code></pre> <p>The API should be syntactic sugar over the same logical-plan primitives used by the CLI.</p>"},{"location":"architecture/#2-frontend-adapters","title":"2. Frontend adapters","text":"<p>This layer translates frontend-specific constructs into the core logical model.</p> <p>Suggested modules:</p> <ul> <li><code>frontend/cli_to_plan.py</code></li> <li><code>frontend/sql_to_plan.py</code></li> <li><code>frontend/python_api.py</code></li> </ul> <p>Responsibilities:</p> <ul> <li>Normalize names, aliases, and option defaults.</li> <li>Validate command-specific syntax.</li> <li>Produce core <code>LogicalPlan</code> nodes.</li> <li>Avoid data access and execution decisions.</li> </ul>"},{"location":"architecture/#3-core-logical-layer","title":"3. Core logical layer","text":"<p>This is the heart of the target architecture.</p>"},{"location":"architecture/#core-entities","title":"Core entities","text":"<ul> <li><code>LogicalPlan</code>: immutable tree or DAG representing the requested transformations.</li> <li><code>LogicalNode</code>: base type for all plan nodes.</li> <li><code>Schema</code>: columns, dtypes, nullability, and optional source lineage.</li> <li><code>LogicalProperties</code>: grouping, ordering, partitioning, row-count estimates, and format hints.</li> <li><code>Expression AST</code>: typed internal representation for user expressions.</li> </ul>"},{"location":"architecture/#canonical-logical-nodes","title":"Canonical logical nodes","text":"<ul> <li><code>Scan</code></li> <li><code>Project</code></li> <li><code>Filter</code></li> <li><code>Mutate</code></li> <li><code>Aggregate</code></li> <li><code>Join</code></li> <li><code>Window</code></li> <li><code>Sort</code></li> <li><code>Limit</code></li> <li><code>Ungroup</code></li> <li><code>View</code></li> <li><code>Sink</code></li> </ul>"},{"location":"architecture/#why-this-layer-matters","title":"Why this layer matters","text":"<p>Today, grouping is represented through schema metadata when tables are written and read back. In the target architecture, grouping becomes a logical property of the plan. That allows the optimizer and execution engine to reason about semantics without depending on transport-specific metadata.</p>"},{"location":"architecture/#4-expression-subsystem","title":"4. Expression subsystem","text":"<p>The current parser is a good start, but the final system should split expression handling into three steps:</p> <ol> <li>Parse user text into an AST.</li> <li>Analyze names, function usage, and types.</li> <li>Compile expressions into backend-specific execution forms.</li> </ol>"},{"location":"architecture/#design-goals","title":"Design goals","text":"<ul> <li>Keep the current approachable expression syntax.</li> <li>Support column references, arithmetic, comparisons, boolean logic, and a curated function set.</li> <li>Compile simple expressions to Arrow compute kernels when possible.</li> <li>Compile advanced expressions to DuckDB SQL when beneficial.</li> <li>Produce stable, user-friendly diagnostics.</li> </ul> <p>Suggested modules:</p> <ul> <li><code>expr/parser.py</code></li> <li><code>expr/analyzer.py</code></li> <li><code>expr/compiler_arrow.py</code></li> <li><code>expr/compiler_duckdb.py</code></li> <li><code>expr/types.py</code></li> </ul>"},{"location":"architecture/#5-optimizer","title":"5. Optimizer","text":"<p>The optimizer is the main performance lever in the final version.</p>"},{"location":"architecture/#required-rule-families","title":"Required rule families","text":""},{"location":"architecture/#projection-pushdown","title":"Projection pushdown","text":"<p>Read only the columns needed downstream, especially for Parquet and ORC scans.</p>"},{"location":"architecture/#filter-pushdown","title":"Filter pushdown","text":"<p>Push predicates into scans and backend-native query execution whenever possible.</p>"},{"location":"architecture/#operator-fusion","title":"Operator fusion","text":"<p>Collapse adjacent transforms such as:</p> <ul> <li><code>select</code> + <code>mutate</code></li> <li>multiple <code>mutate</code> expressions</li> <li>redundant projections</li> <li>unnecessary materializations</li> </ul>"},{"location":"architecture/#logical-simplification","title":"Logical simplification","text":"<p>Remove no-op or redundant nodes such as:</p> <ul> <li><code>ungroup</code> on ungrouped data</li> <li>repeated sorts on identical keys</li> <li><code>select</code> that preserves all columns in order</li> </ul>"},{"location":"architecture/#backend-selection","title":"Backend selection","text":"<p>Choose the right physical backend per plan or plan fragment.</p> <p>Examples:</p> <ul> <li>Arrow backend for vectorized projection/filter/mutate</li> <li>DuckDB backend for heavy joins, SQL, sort-heavy plans, and complex window logic</li> </ul>"},{"location":"architecture/#materialization-policy","title":"Materialization policy","text":"<p>Decide when to keep data lazy, when to stream batches, and when to materialize full tables.</p>"},{"location":"architecture/#6-execution-engine","title":"6. Execution engine","text":"<p>The execution engine transforms optimized logical plans into executable physical plans.</p>"},{"location":"architecture/#responsibilities","title":"Responsibilities","text":"<ul> <li>Build physical operators from logical nodes.</li> <li>Pick backends and split the plan into backend-compatible fragments.</li> <li>Manage intermediate representation boundaries.</li> <li>Guarantee stable output contracts such as <code>pa.Table</code> or a standardized <code>ExecutionResult</code>.</li> </ul>"},{"location":"architecture/#recommended-abstraction","title":"Recommended abstraction","text":"<pre><code>LogicalPlan -&gt; OptimizedPlan -&gt; PhysicalPlan -&gt; ExecutionResult\n</code></pre>"},{"location":"architecture/#executionresult-contract","title":"ExecutionResult contract","text":"<p>Use a normalized result wrapper with helpers such as:</p> <ul> <li><code>to_table()</code></li> <li><code>to_batches()</code></li> <li><code>write_to_sink()</code></li> <li><code>schema</code></li> <li><code>stats</code></li> </ul> <p>This prevents backend-specific return types from leaking into tests and callers.</p>"},{"location":"architecture/#7-backend-layer","title":"7. Backend layer","text":""},{"location":"architecture/#arrow-backend","title":"Arrow backend","text":"<p>Use Arrow-native execution for:</p> <ul> <li>scans and sinks</li> <li><code>select</code></li> <li><code>filter</code></li> <li><code>mutate</code></li> <li>simple aggregations</li> <li>lightweight transformations on in-memory tables</li> </ul> <p>Advantages:</p> <ul> <li>tight integration with current code</li> <li>minimal overhead</li> <li>predictable vectorized execution</li> </ul>"},{"location":"architecture/#duckdb-backend","title":"DuckDB backend","text":"<p>Use DuckDB for:</p> <ul> <li>SQL frontends and SQL fallbacks</li> <li>large joins</li> <li>ordering and sorting</li> <li>advanced window functions</li> <li>plans where relational optimization is materially better than custom execution</li> </ul>"},{"location":"architecture/#hybrid-execution","title":"Hybrid execution","text":"<p>In the final optimized version, the planner should be allowed to run a plan partly in Arrow and partly in DuckDB, with Arrow Tables as the interchange contract.</p>"},{"location":"architecture/#8-io-adapters","title":"8. I/O adapters","text":"<p>I/O should remain isolated from planning and transformation logic.</p>"},{"location":"architecture/#responsibilities_1","title":"Responsibilities","text":"<ul> <li>format detection</li> <li>schema inference</li> <li>scan options</li> <li>sink configuration</li> <li>transport details for files, pipes, and future remote storage</li> </ul>"},{"location":"architecture/#suggested-package-structure","title":"Suggested package structure","text":"<pre><code>io/\n  scan/\n    csv.py\n    parquet.py\n    feather.py\n    orc.py\n  sink/\n    csv.py\n    parquet.py\n    feather.py\n    orc.py\n  formats.py\n  options.py\n</code></pre>"},{"location":"architecture/#future-extension-points","title":"Future extension points","text":"<ul> <li>Arrow IPC streams for process-to-process transfer</li> <li>directory-based dataset scans</li> <li>remote object storage adapters</li> <li>partition discovery</li> </ul>"},{"location":"architecture/#data-and-metadata-model","title":"Data and metadata model","text":""},{"location":"architecture/#current-limitation","title":"Current limitation","text":"<p>Grouping metadata is currently embedded in schema metadata when transporting grouped tables through CSV or Arrow formats.</p>"},{"location":"architecture/#target-model","title":"Target model","text":"<p>Promote execution semantics into a dedicated logical-properties model.</p> <p>Suggested logical properties:</p> <ul> <li><code>group_keys</code></li> <li><code>ordering</code></li> <li><code>partitions</code></li> <li><code>source_format</code></li> <li><code>estimated_rows</code></li> <li><code>estimated_size_bytes</code></li> <li><code>is_materialized</code></li> </ul> <p>Transport metadata may still exist for interoperability, but it should never be the only source of truth for plan semantics.</p>"},{"location":"architecture/#performance-strategy","title":"Performance strategy","text":""},{"location":"architecture/#1-lazy-planning-by-default","title":"1. Lazy planning by default","text":"<p>Even when invoked from a CLI pipeline, <code>barrow</code> should attempt to build a larger logical plan before executing.</p>"},{"location":"architecture/#2-batch-aware-execution","title":"2. Batch-aware execution","text":"<p>Support chunked processing for operations that do not require full materialization.</p> <p>Good streaming candidates:</p> <ul> <li><code>select</code></li> <li><code>filter</code></li> <li>some <code>mutate</code> operations</li> <li>format conversion</li> </ul> <p>Materialization-required operators:</p> <ul> <li>global sort</li> <li>many joins</li> <li>many window functions</li> <li>some grouped aggregations depending on backend strategy</li> </ul>"},{"location":"architecture/#3-column-pruning","title":"3. Column pruning","text":"<p>Apply column pruning as early as possible to reduce memory pressure and I/O cost.</p>"},{"location":"architecture/#4-backend-aware-optimization","title":"4. Backend-aware optimization","text":"<p>Let the optimizer treat backend capabilities as part of the planning problem.</p>"},{"location":"architecture/#5-stable-interchange-format","title":"5. Stable interchange format","text":"<p>Use Arrow Tables or Arrow RecordBatches as the normalized interchange representation between fragments.</p>"},{"location":"architecture/#error-handling-model","title":"Error-handling model","text":"<p>The final architecture should use typed error domains:</p> <ul> <li><code>FrontendError</code></li> <li><code>ExpressionError</code></li> <li><code>PlanningError</code></li> <li><code>OptimizationError</code></li> <li><code>ExecutionError</code></li> <li><code>IOError</code></li> </ul> <p>Benefits:</p> <ul> <li>clearer diagnostics</li> <li>better testability</li> <li>easier CLI messaging</li> <li>easier API integration</li> </ul>"},{"location":"architecture/#observability-and-explainability","title":"Observability and explainability","text":"<p>To make the engine debuggable and trustworthy, add:</p> <ul> <li><code>barrow explain ...</code> to print logical and physical plans</li> <li>execution stats per operator</li> <li>backend choice annotations</li> <li>optional timing and row-count output</li> <li>debug logging hooks around optimization decisions</li> </ul>"},{"location":"architecture/#testing-strategy-for-the-target-architecture","title":"Testing strategy for the target architecture","text":""},{"location":"architecture/#unit-tests","title":"Unit tests","text":"<ul> <li>expression parsing and semantic analysis</li> <li>optimizer rule behavior</li> <li>backend adapters</li> <li>logical-plan validation</li> </ul>"},{"location":"architecture/#integration-tests","title":"Integration tests","text":"<ul> <li>end-to-end CLI pipelines</li> <li>mixed-format pipelines</li> <li>SQL and window workflows</li> <li>cross-backend equivalence</li> </ul>"},{"location":"architecture/#golden-tests","title":"Golden tests","text":"<ul> <li>plan rendering for <code>explain</code></li> <li>stable error messages</li> <li>documentation examples</li> </ul>"},{"location":"architecture/#compatibility-tests","title":"Compatibility tests","text":"<ul> <li>verify backend return types normalize correctly</li> <li>verify Arrow/DuckDB version differences do not leak into public behavior</li> </ul>"},{"location":"architecture/#suggested-target-package-layout","title":"Suggested target package layout","text":"<pre><code>barrow/\n  cli/\n    main.py\n    commands/\n      filter.py\n      select.py\n      mutate.py\n      groupby.py\n      summary.py\n      join.py\n      window.py\n      sql.py\n      view.py\n      explain.py\n  frontend/\n    cli_to_plan.py\n    sql_to_plan.py\n    python_api.py\n  core/\n    plan.py\n    nodes.py\n    schema.py\n    properties.py\n    result.py\n    errors.py\n  optimizer/\n    optimizer.py\n    rules/\n      projection_pushdown.py\n      filter_pushdown.py\n      fusion.py\n      simplify.py\n      backend_selection.py\n  execution/\n    engine.py\n    physical_plan.py\n    backends/\n      arrow_backend.py\n      duckdb_backend.py\n      streaming_backend.py\n  expr/\n    parser.py\n    analyzer.py\n    compiler_arrow.py\n    compiler_duckdb.py\n    types.py\n  io/\n    scan/\n      csv.py\n      parquet.py\n      feather.py\n      orc.py\n    sink/\n      csv.py\n      parquet.py\n      feather.py\n      orc.py\n    formats.py\n    options.py\n</code></pre>"},{"location":"architecture/#migration-plan","title":"Migration plan","text":""},{"location":"architecture/#phase-1-stabilize-contracts","title":"Phase 1: stabilize contracts","text":"<ul> <li>Introduce <code>ExecutionResult</code>.</li> <li>Normalize SQL backend return types.</li> <li>Separate frontend parsing from execution entrypoints.</li> <li>Add architecture tests around result contracts.</li> </ul>"},{"location":"architecture/#phase-2-introduce-logical-planning","title":"Phase 2: introduce logical planning","text":"<ul> <li>Create <code>LogicalPlan</code> and initial nodes.</li> <li>Convert CLI commands to plan builders.</li> <li>Keep current eager execution behind the engine as an interim implementation.</li> </ul>"},{"location":"architecture/#phase-3-add-optimization","title":"Phase 3: add optimization","text":"<ul> <li>Implement projection pushdown.</li> <li>Implement filter pushdown.</li> <li>Add operator fusion.</li> <li>Add backend selection rules.</li> </ul>"},{"location":"architecture/#phase-4-expose-new-frontends","title":"Phase 4: expose new frontends","text":"<ul> <li>Add <code>sql</code> CLI command backed by the common engine.</li> <li>Add <code>window</code> CLI command through the planner.</li> <li>Add <code>explain</code> command.</li> <li>Add optional Python API.</li> </ul>"},{"location":"architecture/#phase-5-advanced-execution","title":"Phase 5: advanced execution","text":"<ul> <li>Add chunked execution where possible.</li> <li>Add dataset scans and partition-aware reads.</li> <li>Add richer observability and plan stats.</li> </ul>"},{"location":"architecture/#recommended-immediate-next-steps","title":"Recommended immediate next steps","text":"<ol> <li>Create a <code>core.result.ExecutionResult</code> abstraction.</li> <li>Refactor the CLI so subcommands build plan objects instead of directly reading, executing, and writing.</li> <li>Move grouping semantics from implicit schema metadata to explicit logical properties.</li> <li>Introduce a minimal optimizer with projection and filter pushdown.</li> <li>Add a backend selection layer that chooses Arrow or DuckDB.</li> <li>Expose <code>sql</code> and <code>window</code> only after they run through the same execution contract as the rest of the engine.</li> </ol>"},{"location":"architecture/#final-recommendation","title":"Final recommendation","text":"<p>The ideal final version of <code>barrow</code> is not just a collection of CLI subcommands. It is a lightweight analytical engine with:</p> <ul> <li>one logical model,</li> <li>one optimization pipeline,</li> <li>multiple frontends,</li> <li>multiple execution backends,</li> <li>and Arrow as the interoperability spine.</li> </ul> <p>That architecture keeps the current usability of the project while giving it a credible path toward better performance, cleaner extensibility, and a more coherent product surface.</p>"},{"location":"architecture/#phase-1-implementation-status","title":"Phase 1 implementation status","text":"<p>The following components from the target architecture have been implemented:</p>"},{"location":"architecture/#completed","title":"Completed","text":"<ul> <li>Core logical layer: <code>LogicalNode</code>, <code>LogicalPlan</code>, <code>ExecutionResult</code>, <code>LogicalProperties</code></li> <li>Optimizer with rules: simplify, fusion, filter pushdown, projection pushdown, backend selection</li> <li>Execution engine with Arrow and DuckDB backends</li> <li>Frontend adapters: <code>cli_to_plan</code>, <code>sql_to_plan</code></li> <li>I/O adapters: per-format scan and sink modules</li> <li>Expression enhancements: <code>analyzer.py</code>, <code>compiler.py</code></li> <li>New CLI commands: <code>sort</code>, <code>sql</code>, <code>window</code>, <code>explain</code></li> <li>Typed error hierarchy in <code>barrow.core.errors</code></li> <li>All existing CLI commands rewired through the plan-based pipeline</li> </ul>"},{"location":"architecture/#next-steps-phase-2","title":"Next steps (Phase 2)","text":"<ul> <li>Active projection/filter pushdown in scans</li> <li>Lazy execution mode</li> <li>Backend selection based on cost estimation</li> <li>Chunked/streaming execution for stateless operations</li> </ul>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11 or later</li> <li>DuckDB and Apache Arrow are required. They are installed automatically when using <code>pip</code> but can also be installed system wide for optimal performance.</li> </ul>"},{"location":"installation/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install barrow\n</code></pre> <p>This command installs <code>barrow</code> along with its core dependencies such as <code>pyarrow</code>, <code>duckdb</code> and <code>numpy</code>.</p>"},{"location":"installation/#development-install","title":"Development Install","text":"<p>For contributing or running the test suite, install the optional development and documentation dependencies:</p> <pre><code>pip install -e .[dev,docs]\n</code></pre> <p>This installs linting tools, test runners and documentation tooling like MkDocs.</p>"},{"location":"installation/#using-the-setup-script","title":"Using the setup script","text":"<p>To create an isolated development environment with shell completion, run:</p> <pre><code>source scripts/setup_env.sh\n</code></pre> <p>This creates a virtual environment in <code>.venv</code>, installs development dependencies and enables <code>barrow</code> tab completion for the current shell.</p>"},{"location":"installation/#running-with-docker","title":"Running with Docker","text":"<p>A minimal Dockerfile is provided to run <code>barrow</code> in a container:</p> <pre><code>docker build -t barrow .\ndocker run --rm -it barrow --help\n</code></pre>"},{"location":"installation/#verifying-the-installation","title":"Verifying the Installation","text":"<p>After installation, verify that the CLI works:</p> <pre><code>barrow --help\n</code></pre> <p>The command should print the top-level help screen describing available subcommands.</p>"},{"location":"manual/","title":"Manual","text":"<p><code>barrow</code> provides subcommands for working with tabular data. Each command can read from <code>STDIN</code> or a file and write to <code>STDOUT</code> or a file.</p>"},{"location":"manual/#common-io-options","title":"Common I/O options","text":"<ul> <li><code>-i</code>, <code>--input PATH</code> \u2013 input file. Reads <code>STDIN</code> if omitted.</li> <li><code>--input-format {csv,parquet,feather,orc}</code> \u2013 format of the input file.</li> <li><code>-o</code>, <code>--output PATH</code> \u2013 output file. Writes to <code>STDOUT</code> if omitted.</li> <li><code>--output-format {csv,parquet,feather,orc}</code> \u2013 format of the output. Defaults to the input format and is ignored by <code>view</code>.</li> <li><code>--csv</code>, <code>--parquet</code>, <code>--feather</code>, <code>--orc</code> \u2013 shortcut flags to set the output format.</li> <li><code>--delimiter CHAR</code> \u2013 field delimiter for CSV input; also used for output unless <code>--csv-out-delimiter</code> is given.</li> <li><code>--csv-out-delimiter CHAR</code> \u2013 field delimiter for CSV output.</li> <li><code>--tmp</code> \u2013 write intermediate results to Feather when using pipes for faster processing.</li> </ul>"},{"location":"manual/#filter","title":"filter","text":"<p>Filter rows using a boolean expression.</p> <pre><code>barrow filter \"score &gt; 80\" -i data.csv -o filtered.csv\n</code></pre>"},{"location":"manual/#select","title":"select","text":"<p>Select a comma-separated list of columns.</p> <pre><code>barrow select \"name,score\" -i data.csv -o subset.csv\n</code></pre>"},{"location":"manual/#mutate","title":"mutate","text":"<p>Add or modify columns with <code>NAME=EXPR</code> pairs.</p> <pre><code>barrow mutate \"total=price*qty\" -i sales.csv -o extended.csv\n</code></pre>"},{"location":"manual/#groupby","title":"groupby","text":"<p>Group rows by columns.</p> <pre><code>barrow groupby category -i sales.csv | barrow summary \"revenue=sum(total)\"\n</code></pre> <p>Grouped data keeps track of the grouping so that subsequent operations like <code>summary</code> can aggregate correctly.</p>"},{"location":"manual/#summary","title":"summary","text":"<p>Aggregate a grouped table with <code>COLUMN=AGG</code> pairs.</p> <pre><code>barrow groupby category -i sales.csv | barrow summary \"revenue=sum(total)\"\n</code></pre>"},{"location":"manual/#ungroup","title":"ungroup","text":"<p>Remove grouping metadata.</p> <pre><code>barrow groupby category -i data.csv | barrow ungroup\n</code></pre>"},{"location":"manual/#join","title":"join","text":"<p>Join two tables on key columns.</p> <pre><code>barrow join id id --right other.csv -i left.csv -o joined.csv\n</code></pre> <p>Additional options:</p> <ul> <li><code>--right PATH</code> \u2013 right input file.</li> <li><code>--right-format {csv,parquet,feather,orc}</code> \u2013 format of the right file.</li> <li><code>--join-type {inner,left,right,outer}</code> \u2013 type of join (default <code>inner</code>).</li> </ul>"},{"location":"manual/#view","title":"view","text":"<p>Display a table in CSV format to <code>STDOUT</code> for inspection.</p> <pre><code>barrow view -i data.parquet\n</code></pre> <p><code>view</code> accepts <code>--output-format</code> for API compatibility but always writes CSV to <code>STDOUT</code>.</p>"},{"location":"manual/#sort","title":"sort","text":"<p>Sort rows by column values.</p> <pre><code>barrow sort COLUMNS [options]\n</code></pre> <p>COLUMNS: comma-separated column names to sort by.</p> <ul> <li><code>--desc</code>: sort in descending order.</li> </ul> <pre><code>barrow sort 'name,age' -i people.csv\n</code></pre> <pre><code>barrow sort 'age' --desc -i people.csv -o sorted.csv\n</code></pre>"},{"location":"manual/#sql","title":"sql","text":"<p>Execute a SQL query.</p> <pre><code>barrow sql QUERY [options]\n</code></pre> <p>QUERY: SQL query where the input table is named <code>tbl</code>.</p> <pre><code>barrow sql 'SELECT name, age FROM tbl WHERE age &gt; 30' -i people.csv\n</code></pre> <pre><code>barrow sql 'SELECT grp, SUM(a) AS total FROM tbl GROUP BY grp' -i data.csv\n</code></pre>"},{"location":"manual/#window","title":"window","text":"<p>Apply window functions.</p> <pre><code>barrow window ASSIGNMENTS [--by COLS] [--order-by COLS] [options]\n</code></pre> <p>ASSIGNMENTS: comma-separated <code>NAME=EXPR</code> pairs.</p> <ul> <li><code>--by</code>: partition columns (comma-separated).</li> <li><code>--order-by</code>: order columns within partitions (comma-separated).</li> </ul> <pre><code>barrow window 'rn=row_number()' --by grp --order-by val -i data.csv\n</code></pre> <pre><code>barrow window 'ma=rolling_mean(value, 3)' --order-by date -i timeseries.csv\n</code></pre>"},{"location":"manual/#explain","title":"explain","text":"<p>Show execution plan.</p> <pre><code>barrow explain COMMAND [EXPRESSION] [options]\n</code></pre> <p>COMMAND: the command to explain (filter, select, sort, etc.). EXPRESSION: the expression or columns for that command.</p> <pre><code>barrow explain filter 'age &gt; 30' -i people.csv\n</code></pre>"},{"location":"manual/#benchmark-de-comandos","title":"Benchmark de comandos","text":"<p>Para comparar opera\u00e7\u00f5es individuais e pipelines completos, use <code>scripts/benchmark.sh</code>. O script cria automaticamente arquivos CSV determin\u00edsticos de teste, aceita os datasets opcionais <code>tiny</code> e <code>xlarge</code> al\u00e9m dos tamanhos padr\u00e3o <code>small</code>, <code>medium</code> e <code>large</code>, mede varia\u00e7\u00f5es em fases expl\u00edcitas de <code>cold</code>, <code>warmup</code> e <code>hot</code>, usa o mesmo total de <code>--iterations</code> para cada fase habilitada de uma variante, adiciona equivalentes em SQL para as opera\u00e7\u00f5es b\u00e1sicas quando fizer sentido, captura tempo total, pico de mem\u00f3ria RSS e tempo de CPU, e registra os tempos detalhados em <code>results.tsv</code>, al\u00e9m de gerar <code>summary.md</code> e <code>summary.json</code> no diret\u00f3rio de trabalho escolhido.</p> <pre><code>scripts/benchmark.sh --datasets small,medium --iterations 5\nscripts/benchmark.sh --datasets tiny,xlarge --only filter,pipeline --iterations 2\n</code></pre> <p>Se quiser focar em grupos espec\u00edficos, use <code>--only</code>, por exemplo <code>--only filter,sql,pipeline</code>. Use <code>--warmup</code> e <code>--cold-runs</code> para habilitar ou desabilitar as fases preparat\u00f3rias, <code>--iterations</code> para controlar quantas execu\u00e7\u00f5es cada fase habilitada registra por variante, <code>--sizes</code> para sobrescrever os row counts usando <code>small:medium:large</code> ou <code>tiny:small:medium:large:xlarge</code>, e <code>--cleanup</code> para remover os artefatos gerados ao final mantendo os resultados e summaries.</p>"},{"location":"usage/","title":"Usage","text":"<p><code>barrow</code> operates on tabular data using a pipeline of commands connected with standard Unix pipes.</p>"},{"location":"usage/#basic-concepts","title":"Basic Concepts","text":"<p>Each command reads from <code>STDIN</code> or a file and writes to <code>STDOUT</code> or a file. Formats are inferred from file extensions or may be specified explicitly with <code>--input-format</code> and <code>--output-format</code>.</p>"},{"location":"usage/#advanced-examples","title":"Advanced Examples","text":""},{"location":"usage/#join-and-aggregate-with-orc-output","title":"Join and Aggregate with ORC Output","text":"<pre><code>barrow join id id --right other.csv --input data.csv --input-format csv --right-format csv --delimiter ';' | \\\nbarrow mutate \"total=price*qty\" --tmp | \\\nbarrow groupby category --tmp | \\\nbarrow summary \"revenue=sum(total)\" --orc --output report.orc\n</code></pre> <p>This pipeline joins two semicolon-delimited CSV datasets on <code>id</code>, uses <code>--tmp</code> to store intermediate results in Feather, groups by <code>category</code>, and writes aggregated revenue to an ORC file. When writing grouped data to CSV, grouping information is stored in a leading comment line of the form <code># grouped_by: col1,col2</code> and is restored on read.</p>"},{"location":"usage/#streaming-filters-and-projections","title":"Streaming Filters and Projections","text":"<pre><code>barrow filter \"score &gt; 80\" --input-format csv | \\\nbarrow select \"name,score\" | \\\nbarrow sort \"score\" --descending --output-format parquet --output top.parquet\n</code></pre> <p>Use streaming operations to filter, project, and sort large datasets without loading them entirely into memory.</p>"},{"location":"usage/#filter-mutate-select-groupby-summary","title":"Filter \u2192 mutate \u2192 select \u2192 groupby \u2192 summary","text":"<pre><code># filter, mutate, select, groupby, and summarize\nbarrow filter \"a &gt; 1\" --input data.csv --input-format csv | \\\nbarrow mutate \"c=a+b\" | \\\nbarrow select \"c,grp\" | \\\nbarrow groupby grp | \\\nbarrow summary \"c=sum\"\n</code></pre> <pre><code>grp,c_sum\nx,7\ny,9\n</code></pre>"},{"location":"usage/#inspecting-results","title":"Inspecting Results","text":"<p>When working with binary formats like Parquet, append <code>view</code> to a pipeline to inspect the data in a human-readable form:</p> <pre><code>barrow summary \"revenue=sum(total)\" --output-format parquet | \\\nbarrow view\n</code></pre> <p><code>view</code> ignores <code>--output-format</code> and always prints text (CSV) to <code>STDOUT</code>. It can also read directly from a file:</p> <pre><code>barrow view -i data.parquet\n</code></pre>"},{"location":"usage/#new-commands","title":"New Commands","text":""},{"location":"usage/#sort","title":"Sort","text":"<pre><code>barrow sort 'name,age' -i people.csv\n</code></pre> <p>Sort in descending order and write to a file:</p> <pre><code>barrow sort 'age' --desc -i people.csv -o sorted.csv\n</code></pre>"},{"location":"usage/#sql-queries","title":"SQL Queries","text":"<p>Run a SQL query against the input table (referenced as <code>tbl</code>):</p> <pre><code>barrow sql 'SELECT name, age FROM tbl WHERE age &gt; 30' -i people.csv\n</code></pre> <p>Aggregate with SQL:</p> <pre><code>barrow sql 'SELECT grp, SUM(a) AS total FROM tbl GROUP BY grp' -i data.csv\n</code></pre>"},{"location":"usage/#window-functions","title":"Window Functions","text":"<p>Add a row number partitioned by group and ordered by value:</p> <pre><code>barrow window 'rn=row_number()' --by grp --order-by val -i data.csv\n</code></pre> <p>Compute a rolling mean over a time series:</p> <pre><code>barrow window 'ma=rolling_mean(value, 3)' --order-by date -i timeseries.csv\n</code></pre>"},{"location":"usage/#explain-plan","title":"Explain Plan","text":"<p>Inspect the execution plan for a command without running it:</p> <pre><code>barrow explain filter 'age &gt; 30' -i people.csv\n</code></pre>"},{"location":"usage/#performance-tips","title":"Performance Tips","text":"<ul> <li>Prefer the Parquet format for large datasets to leverage Arrow's columnar layout.</li> <li>Provide explicit <code>--input-format</code> and <code>--output-format</code> to avoid format detection overhead.</li> <li>Use <code>select</code> early in pipelines to reduce the number of processed columns.</li> <li>When possible, install DuckDB and Arrow libraries with SIMD support for better throughput.</li> </ul>"}]}